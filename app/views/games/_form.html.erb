<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :white_offense %><br />
    <%= f.select :white_offense_id, options_from_collection_for_select(@players, 'id', 'name') %>
  </div>
  <div class="field">
    <%= f.label :white_defense %><br />
    <%= f.select :white_defense_id, options_from_collection_for_select(@players, 'id', 'name') %>
  </div>
  <div class="field">
    <%= f.label :blue_offense %><br />
    <%= f.select :blue_offense_id, options_from_collection_for_select(@players, 'id', 'name') %>
  </div>
  <div class="field">
    <%= f.label :blue_defense %><br />
    <%= f.select :blue_defense_id, options_from_collection_for_select(@players, 'id', 'name') %>
  </div>
  <div class="field">
    <%= f.label :white_score %><br />
    <%= f.select :white_score, options_for_select(0..10) %>
  </div>  <div class="field">
    <%= f.label :blue_score %><br />
    <%= f.select :blue_score, options_for_select(0..10) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
