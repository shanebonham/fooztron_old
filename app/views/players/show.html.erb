<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @player.name %>
</p>

<% if @player.hidden? %>
  <p>
    <b><%= @player.name %> is hidden.</b>
  </p>
<% end %>

<% if current_user.present? and current_user.admin? %>
  <%= link_to 'Edit', edit_player_path(@player) %> |
<% end %>
<%= link_to 'Back', players_path %>
